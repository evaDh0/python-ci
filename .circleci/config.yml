version: 2

jobs:
  build:
    docker:
      - image: continuumio/miniconda 
    steps:
      - checkout
      - run:
          name: Hello World test
          command: |
            echo "Hello world"
      - run: 
          name: Install dependencies
          command: |
            conda create -n test-env --file environment.yml 
            source activate test-env
      - run:
          name: Run tests
          command: |
            nosetests
        
